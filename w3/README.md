# Week 3 Summary

During this week, I learned how to build and test a REST API using ASP.NET Core. I implemented CRUD operations, connected the API to a database with Entity Framework Core, and tested all endpoints using Postman. I also organized the requests into a Postman collection, added simple test scripts, used a `baseUrl` environment variable, and documented the project with a README.