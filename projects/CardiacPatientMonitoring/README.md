Patients
GET       → Admin, Doctor
GET/{id}  → Admin, Doctor
POST      → Admin
PUT       → Admin
DELETE    → Admin
VitalSigns
GET       → Admin, Doctor, Patient
GET/{id}  → Admin, Doctor, Patient
POST      → Admin, Doctor
PUT       → Admin, Doctor
DELETE    → Admin
Medications
GET       → Admin, Doctor, Patient
GET/{id}  → Admin, Doctor, Patient
POST      → Admin, Doctor
PUT       → Admin, Doctor
DELETE    → Admin
Appointments
GET       → Admin, Doctor, Patient
GET/{id}  → Admin, Doctor, Patient
POST      → Admin, Doctor
PUT       → Admin, Doctor
DELETE    → Admin